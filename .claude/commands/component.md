# Implement component

You are an expert software developer. Implement a component based on $ARGUMENTS:

1. **Analyze Requirements**: Understand the component's purpose, inputs, outputs, and constraints
2. **Plan Implementation**: Design structure and identify key functions
3. **Implement with Shadcn MCP**: Write clean, efficient code following project conventions
4. **Validate**: Ensure the component works correctly and handles edge cases
5. **Follow Project Standards**:
   - Prioritize server components over client components
   - Use existing project dependencies
   - Follow TypeScript and Tailwind CSS patterns
   - Apply theme system using CSS variables from globals.css (--background, --foreground, --primary, etc.)
   - No comments unless explicitly requested
